# Stakeholder links (copy into Confluence / Jira)

Use the canonical table in the repository: **[`PROTOTYPES.md`](../PROTOTYPES.md)** — update it whenever Vibesharing URLs change.

Suggested wording for a Confluence “Prototypes” callout:

- **Milestone MVP (schema viewer + schema management):** [paste MVP deploy URL after connecting `custom-attributes-mvp/` in Vibesharing]
- **Full vision (roles, workflows, primary schema management):** [paste vision deploy URL — `custom-attributes-v2/`]
- **Lab (experimental workflows and explorations only):** [paste lab deploy URL — `custom-attributes-lab/`]

Optional: set `VITE_MVP_PROTOTYPE_URL` on the vision deploy and `VITE_VISION_PROTOTYPE_URL` on the MVP deploy so the in-app banners link to each other.
